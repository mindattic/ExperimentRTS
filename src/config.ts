/** Radius of the desert planet, in scene units (meters). */
export const PLANET_RADIUS = 2000;

/** Seed for the procedural terrain (noise + biome layout). Change to regenerate the planet. */
export const PLANET_SEED = 1337;
